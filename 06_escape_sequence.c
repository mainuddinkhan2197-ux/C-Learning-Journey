#include<stdio.h>

int main(){

    int a;
    char ch= 'a';
    ch = 'c';
    ch = '\n'; // represents a new line
    printf(" Hey I am good \' nice"); // here we use bacslash(\) with any 
    // word so like backslash n or backslash t we also use
    return 0;
}